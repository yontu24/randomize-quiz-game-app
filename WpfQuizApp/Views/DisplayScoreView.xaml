<UserControl x:Class="WpfQuizApp.Views.DisplayScoreView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfQuizApp.Views"
        mc:Ignorable="d"
        Height="450"
        Width="800" FontFamily="Verdana">

    <StackPanel>
        <TextBlock Text="{Binding Result1}" FontWeight="Bold" FontSize="20pt" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding Result2}" HorizontalAlignment="Center"/>
        <Button Content="Try again"
                Margin="10,10,10,10"
                Width="150"
                Command="{Binding CommandTryAgain}"/>
    </StackPanel>

</UserControl>
